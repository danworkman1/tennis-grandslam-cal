# Dependencies
node_modules/

# Generated by `wrangler types` from wrangler.jsonc; regenerated by the
# pre* script hooks, so it is never committed.
worker-configuration.d.ts

# Wrangler local state & build output
.wrangler/
dist/
worker/

# Local secrets / env (never commit these)
.dev.vars
.dev.vars.*
.env
.env.*

# Claude Code local settings (may contain machine-specific paths / contacts)
.claude/settings.local.json

# Logs & OS cruft
*.log
.DS_Store

# hindsight-agent review artifacts
hindsight-agent-reviews.log
hindsight-agent-review-cache.json

docs